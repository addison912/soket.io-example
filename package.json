{
  "name": "websocket-practice",
  "version": "1.0.0",
  "description": "playing with socket.io",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "dev": "parcel src/*.html",
    "build": "parcel build --no-cache src/*.html --dist-dir ./build/",
    "clear": "rm -rf dist .parcel-cache/ build"
  },
  "keywords": [],
  "author": "Addison Moore",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^7.0.0",
    "express": "^4.16.4",
    "socket.io": "^4.0.0",
    "socket.io-client": "^4.0.0"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.2"
  }
}
